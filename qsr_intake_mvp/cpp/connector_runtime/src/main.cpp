#include "Connector.hpp"
#include <iostream>

int main() {
    std::cout << "QSR connector runtime scaffold" << std::endl;
    std::cout << "This binary is the host/runtime shell for long-running connectors." << std::endl;
    std::cout << "For the MVP, Python adapters may be invoked by this runtime or run alongside it." << std::endl;
    return 0;
}
